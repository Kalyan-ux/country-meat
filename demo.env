<div *ngIf="product; else notFound" class="container py-4">
  <nav aria-label="breadcrumb" class="mb-4">
    <ol class="breadcrumb">
      <li class="breadcrumb-item">
        <a routerLink="/"><i class="fas fa-home"></i> Home</a>
      </li>
      <li class="breadcrumb-item">
        <a routerLink="/products/chicken">Country Chicken</a>
      </li>
      <li class="breadcrumb-item active" aria-current="page">
        {{ product.name }}
      </li>
    </ol>
  </nav>

  <div class="row g-5">
    <div class="col-lg-6">
      <div class="product-image-gallery">
        <div class="main-image-container">
          <img
            [src]="selectedImage"
            [alt]="product.name + ' main image'"
            class="main-image"
          />
        </div>
        <div class="thumbnail-column">
          <img
            *ngFor="let img of allImages"
            [src]="img"
            [alt]="product.name + ' thumbnail'"
            class="thumbnail-item"
            [class.active]="img === selectedImage"
            (click)="selectImage(img)"
          />
        </div>
      </div>
    </div>
    <div class="col-lg-6">
      <h1 class="product-title">
        {{ product.name }}
        <span class="special-note" *ngIf="product.specialNote"
          >({{ product.specialNote }})</span
        >
      </h1>
      <p class="product-subtitle">{{ product.subtitle }}</p>

      <p class="product-description">
        <span *ngIf="!isDescriptionExpanded">{{ product.shortdescription }}</span>
        <span *ngIf="isDescriptionExpanded">{{ product.description }}</span>
        <br />
        <a (click)="toggleDescription()" class="read-more-link">
          {{ isDescriptionExpanded ? "Read Less." : "Read More." }}
        </a>
      </p>

      <div class="info-badges">
        <span class="badge-item"
          ><i class="fas fa-calendar-alt"></i> {{ product.age }}</span
        >
        <span class="badge-item"
          ><i class="fas fa-weight-hanging"></i> {{ product.weight }}</span
        >
        <span class="badge-item"
          ><i class="fas fa-users"></i> Serves {{ product.serves }}</span
        >
        <span class="badge-item"
          ><i class="fas fa-drumstick-bite"></i> Meat:
          {{ product.meatType }}</span
        >
        <h2 class="product-description">Note: {{ product.note }}</h2>
      </div>

      <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
          <div class="price-section">
            <span class="current-price">₹{{ product.price }}</span>
            <span
              class="discount"
              *ngIf="product.originalPrice > product.price"
            >
              {{ getDiscount(product.originalPrice, product.price) }}% off
            </span>
          </div>

          <p class="mrp-text mb-0" *ngIf="product.originalPrice > product.price">
            MRP: ₹{{ product.originalPrice }} (Incl. of all taxes)
          </p>

          <div
            class="mrp-block-no-discount mb-0"
            *ngIf="product.originalPrice <= product.price"
          >
            <span class="mrp-main-text"
              >MRP: <strong>₹{{ product.price }}</strong></span
            >
            <small class="mrp-subtext">(incl. of all taxes)</small>
          </div>
        </div>

        <div>
          <div class="action-section">
            <div *ngIf="!product.isOutOfStock">
              <button
                *ngIf="productQuantity === 0"
                (click)="addToCart()"
                class="btn btn-add"
              >
                Add +
              </button>

              <div *ngIf="productQuantity > 0" class="quantity-stepper">
                <button (click)="decreaseQuantity()" class="stepper-btn">
                  &ndash;
                </button>
                <span class="quantity-display">{{ productQuantity }}</span>
                <button (click)="increaseQuantity()" class="stepper-btn">
                  +
                </button>
              </div>
            </div>
            <div *ngIf="product.isOutOfStock" class="out-of-stock-section">
              <button class="btn btn-notify">
                <i class="fas fa-bell"></i> Notify
              </button>
            </div>
          </div>
        </div>
      </div>

      <div *ngIf="product.isOutOfStock" class="out-of-stock-section-message">
        <p class="out-of-stock-text">Out of stock</p>
      </div>
    </div>
  </div>
</div>

<div class="features-benefits-tabs mb-5 col-lg-10 mx-auto">
  <div class="tabs-nav d-flex justify-content-center">
    <button class="btn tab-btn active">Health Benefits</button>
  </div>

  <div class="tabs-content p-4">
    <ul class="list-unstyled mb-0">
      <li
        *ngFor="let benefit of product.healthBenefits"
        class="d-flex align-items-start mb-3"
      >
        <i class="fas fa-check-circle text-success me-3 mt-1"></i>
        <span>{{ benefit }}</span>
      </li>
    </ul>
  </div>
</div>

<div class="row mt-4">
  <div class="col-lg-10 mx-auto related-products-section">
    <h4 class="mb-4">You may also Like</h4>

    <div class="related-products-wrapper">
      <button
        class="scroll-btn left-btn"
        (click)="scrollLeft()"
        *ngIf="showLeftArrow"
      >
        <i class="fas fa-chevron-left"></i>
      </button>

      <div
        #relatedProductsContainer
        class="related-products-container"
        (scroll)="onContainerScroll()"
      >
        <div class="related-product-card" *ngFor="let r of relatedProducts">
          <div class="card h-100 shadow-sm">
            <a
              [routerLink]="['/products/product-details']"
              [queryParams]="{ id: r.id }"
              class="text-decoration-none d-block"
            >
              <img [src]="r.images" class="card-img-top" [alt]="r.name" />
              <div class="card-body">
                <h6 class="card-title text-dark">{{ r.name }}</h6>
                <h6 class="card-subtitle mb-2 text-muted">
                  {{ r.subtitle }}
                </h6>
                <p class="card-text mb-0">
                  <span class="fs-5 fw-bold text-dark">₹{{ r.price }}</span>
                </p>
              </div>
            </a>
          </div>
        </div>
      </div>
      <button
        class="scroll-btn right-btn"
        (click)="scrollRight()"
        *ngIf="showRightArrow"
      >
        <i class="fas fa-chevron-right"></i>
      </button>
    </div>
  </div>
</div>

<ng-template #notFound>
  <div class="text-center py-5">
    <h3>Product not found</h3>
    <p>Sorry, the product you are looking for does not exist.</p>
  </div>
</ng-template>